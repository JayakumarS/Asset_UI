import { PaymentHistoryList } from './payment-history-list.model';

describe('PaymentHistoryList', () => {
  it('should create an instance', () => {
    expect(new PaymentHistoryList()).toBeTruthy();
  });
});
