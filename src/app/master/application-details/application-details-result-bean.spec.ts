import { ApplicationDetailsResultBean } from './application-details-result-bean';

describe('ApplicationDetailsResultBean', () => {
  it('should create an instance', () => {
    expect(new ApplicationDetailsResultBean()).toBeTruthy();
  });
});
